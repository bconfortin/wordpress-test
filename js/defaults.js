$.extend($.fn.dataTable.defaults, {
    language: {
        //url: String.format('{0}Content/js/datatables/locale/{1}.js', window.BASEURL, window.currentCulture)
        url:('/Content/js/datatables/locale/pt-BR.js')
    }
});